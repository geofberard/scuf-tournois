export const now = () => {
    // const date = new Date();
    // date.setHours(15);
    // date.setMinutes(50);
    // date.setSeconds(0);
    // date.setMilliseconds(0);
    return new Date(Date.now());
}